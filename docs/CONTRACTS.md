# Tool contracts

There are no active tool contracts. The current build registers zero tools.

## Planned read-only slice

- `index_site` will build or refresh a local, block-level site index in the browser.
- `find_site` will search the local index and return page, section, and block locations.
- `read_site` will get current data from Squarespace and update the matching local records.

These names describe the next slice. They are not available yet. This slice will not include tools that write to Squarespace.
