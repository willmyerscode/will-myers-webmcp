{
  "name": "will-myers-webmcp",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "node --test",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "fake-indexeddb": "^6.2.5",
    "linkedom": "^0.18.13",
    "typescript": "^5.9.2",
    "wrangler": "^4.32.0"
  }
}
